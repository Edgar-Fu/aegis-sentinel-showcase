# GitHub release checklist

- [ ] Run `scripts\clean.ps1` so no generated evidence or build output is included.
- [ ] Run `scripts\verify.ps1` and confirm all 18 checks pass.
- [ ] Confirm `git status` contains no runtime evidence, IDE state, keys, or local paths.
- [ ] Create a public repository named `aegis-sentinel-showcase` under `Edgar-Fu`.
- [ ] Do not initialize the remote repository with another README, license, or `.gitignore`.
- [ ] Push the provided repository as the initial commit.
- [ ] Add the repository description and topics listed in the owner publication guide.
- [ ] Enable GitHub Pages from `main` and `/docs`.
- [ ] Confirm the Pages site loads at `https://edgar-fu.github.io/aegis-sentinel-showcase/`.
- [ ] Confirm the Actions verification workflow passes.
- [ ] Add the Pages URL to the repository Website field.
- [ ] Pin the repository on the GitHub profile.
- [ ] Add the GitHub repository and Pages URL to LinkedIn/project materials.
